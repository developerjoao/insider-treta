public class Main {
    public static void main(String[] args) {
        MainWindow mainWindow = new MainWindow();
        // User user = new User("Paulo", "asdf", "google.com", "paulo@gmail.com",
        // "vagabundo");

        // Node<User> node = new Node<User>(user);

        // Tree tree = new Tree();
        // tree.addUser(node);
    }
}